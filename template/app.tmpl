{{define "body"}}
  <div id="navbar" class="navbar-color">
    <div class="navbar-container grid-container _flex-row _main-space-between _cross-stretch">

      <a href="/" class="_flex-row _cross-center">
        <img class="logo" src="/~/img/acourse-logo-w-2.svg" height="45">
      </a>
      <div class="_flex-row _cross-center _main-end _flex-span">
      {{if .Page.Me}}
        <div class="dropdown _full-height">
          <div class="dropbtn _full-height _flex-row _cross-center ">
            <img
              class="acourse-block-big acourse-circle _img-cover _no-margin"
              src="{{.Page.Me.Image}}"
              onerror="this.src = '/~/img/placeholder-img.svg'">
            <div class="username">&nbsp;&nbsp;{{.Page.Me.Username}}</div>
            &nbsp;&nbsp;<i class="fa fa-caret-down"></i>
          </div>
          <div class="dropdown-content-w">
            <div class="dropdown-content">
              <a class="_font-main _font-bold _font-size-big {{if eq templateName "profile"}} active{{end}}" href="/profile">Profile</a>
              {{if .Page.Me.Role.Admin}}
              <a class="_font-main _font-bold _font-size-big {{if eq templateName "user"}} active{{end}}" href="/admin/users">Users</a>
              <a class="_font-main _font-bold _font-size-big {{if eq templateName "course"}} active{{end}}" href="/admin/courses">Courses</a>
              <a class="_font-main _font-bold _font-size-big" href="/admin/payments/pending">Pending Payments</a>
              <a class="_font-main _font-bold _font-size-big" href="/admin/payments/history">History Payments</a>
              {{end}}
              <a class="_font-main _font-bold _font-size-big" href="/signout">Sign Out</a>
            </div>
          </div>
        </div>
      {{else}}
        <a class="link _flex-row _cross-center _font-sub" href="/signin">Sign In</a>
        <a href="/signup"><div class="acourse-button -primary _font-sub">Register</div></a>
      {{end}}
      </div>
    </div>
  </div>

  <div class="app-container _bg-color-base">
    {{template "app-body" $}}
  </div>

  <footer class="row">
    <div class="col-xs-12 col-md-6 _font-size-big">
      Acourse.io © 2016-2020 &nbsp &nbsp
      <a href="https://github.com/acoshift/acourse" class="_font-size-bigger" target="_blank">
        <i class="fa fa-github _color-sub"></i>
      </a>
    </div>
    <div class="col-xs-12 col-md-6 _font-size-big">
      Made with &nbsp<i class="fa fa-heart _color-negative pulse"></i>&nbsp;&nbsp;by
      <a class="_color-sub" href="https://moonrhythm.info" target="_blank">Moon Rhythm</a>
    </div>
  </footer>
{{end}}

{{define "script"}}
  <script>
    // Navbar scroll event
    ;(() => {
      let didScroll
      let lastScrollTop = 0
      let delta = 5
      let navbarHeight = $('#navbar').outerHeight()

      $(window).scroll(() => {
        didScroll = true
      })

      const hasScrolled = () => {
        const st = $(this).scrollTop()

        if (Math.abs(lastScrollTop - st) <= delta) return

        if (st > lastScrollTop && st > navbarHeight) {
          $('#navbar').removeClass('nav-down').addClass('nav-up')
        } else {
          if (st + $(window).height() < $(document).height()) {
            $('#navbar').removeClass('nav-up').addClass('nav-down')
          }
        }
        lastScrollTop = st
      }

      setInterval(() => {
        if (didScroll) {
          hasScrolled()
          didScroll = false
        }
      }, 250)
    })()

    function bindFileInputImage (input, img) {
      if (!input || !img) return
      input.onchange = () => {
        const fp = input.files && input.files[0]
        if (fp) {
          const reader = new FileReader()
          reader.onload = (e) => {
            img.src = e.target.result
          }
          reader.readAsDataURL(fp)
        }
      }
    }
  </script>
  {{template "app-script" $}}
{{end}}


{{define "style"}}
{{template "app-style" $}}
<style>
  footer {
    position: relative;
    z-index: 2;
    padding: 48px 32px;
    backface-visibility: hidden;
    background-color: #292d35;
    color: #777777;
  }

  footer > div:first-child {
    text-align: left;
  }

  footer > div:last-child {
    text-align: right;
  }

  @media only screen and (max-width: 767px) {

    footer > div {
      text-align: center !important;
    }

    footer > div:first-child {
      text-align: left;
      margin-bottom: 8px;
    }

  }

  .pulse {
    animation: pound .35s infinite alternate;
    -webkit-animation: pound .35s infinite alternate
  }

  .app-container {
    padding-top: 80px;
  }

  #navbar {
    position: fixed;
    z-index: 100;
    width: 100%;
    top: 0;
    background-color: white;
    transition: top 0.2s ease-in-out;
    border-bottom: 1px solid #e7eef1;
  }

  #navbar .link {
    color: #474f57;
    text-decoration: none;
    padding: 0 16px;
  }

  #navbar.nav-up {
    top: -82px;
  }

  #navbar > .navbar-container {
    position: relative;
    height: 80px;
  }

  #navbar .username {
    display: block !important;
    max-width: 150px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  @media only screen and (max-width: 767px) {
    #navbar .username {
      display: none !important;
    }
  }
  #navbar .acourse-button {
    flex-basis: 100px;
  }

  @-webkit-keyframes pound {
    to {
      transform: scale(1.1)
    }
  }

  @keyframes pound {
    to {
      transform: scale(1.1)
    }
  }

  #navbar .dropbtn {
    border: none;
    cursor: pointer;
  }
  #navbar .dropdown {
    position: relative;
    display: inline-block;
  }
  #navbar .dropdown-content-w {
    padding: 1px 0;
  }
  #navbar .dropdown-content {
    display: none;
    position: absolute;
    background-color: white;
    min-width: 200px;
    border-radius: 3px;
    padding: 8px;
    box-shadow: 2px 10px 20px 0 rgba(46,61,73,0.15);
    z-index: 1;
  }
  #navbar .dropdown-content a {
    color: #7d8e9c;
    display: block;
    padding: 4px 16px;
  }
  #navbar .dropdown-content a:hover,
  #navbar .dropdown-content a.active {
    color: #2F8DFA;
  }
  #navbar .dropdown-content {
    right: 0;
  }
  #navbar .dropdown:hover .dropdown-content {
    display: block;
  }
</style>
{{end}}

